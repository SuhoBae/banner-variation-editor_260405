# 실행계획.md (업데이트 됨)

# AdCanvas 실행 계획 (Roadmap & Todo)

## 🔴 To Do (진행 예정)
- [ ] **에셋 라이브러리 고도화**: 현재 구현된 Asset Tray를 기준으로, 폴더 단위 가져오기 UX 정리, 기본 샘플 세트 제공, 검색/태그/재정렬 등 실제 운영 편의 기능 보강
- [ ] **보드별 변형 UX 다듬기**: 변형된 레이어/보드의 시각 표시 범위 확대, 보드별 오버라이드 비교, 원본/변형 프리셋 복제 등 운영 UX 고도화
- [ ] **아트보드 완전 독립화 검토**: 현재는 공통 레이어 + 보드별 오버라이드 구조를 유지하고 있으므로, 필요 시 보드별 로컬 레이어 생성/정렬/순서까지 완전 독립시키는 아키텍처 확장 검토

## 🟡 In Progress (진행 중)
- [ ] 컴포넌트 구조화: 단일 `App.jsx` 파일이 너무 비대해짐에 따라, Toolbar, LeftPanel, CanvasWorkspace, RightPanel로 분리 검토 (단, 빌드 도구 없이 단일 파일 복붙 편의성을 유지할지 고민 필요)

## 🟢 Done (완료됨)
- [x] **보드 전용 삭제 처리**: 레이어 삭제가 전역 제거되지 않고 현재 아트보드에서만 숨김/제거 의미로 동작하도록 조정
- [x] **Material 기반 UI 리디자인**: 헤더, 좌우 패널, 입력창, 버튼, 트레이를 밝은 Material 계열 표면/상태색/반경/그림자 체계로 리프레시
- [x] **보드별 레이어 속성 변형**: 특정 아트보드에서만 텍스트/CTA 색상/내용/타이포 등을 다르게 저장하고, 오버라이드 리셋 및 보드 단위 초기화로 원본 상태 복귀 가능하도록 구현
- [x] **포인터 기준 줌 + 4방향 리사이즈**: 마우스 포인터 기준으로 확대/축소 위치를 유지하고, 레이어 우하단뿐 아니라 좌우/상하 핸들로도 크기 조절이 가능하도록 개선
- [x] **브랜드 테마 프리셋 제거**: 사용하지 않는 프리셋 기능과 관련 UI를 제거해 편집 흐름을 단순화
- [x] **에셋 라이브러리 연동**: 로컬 다중 이미지 업로드 및 URL 목록 붙여넣기로 에셋을 모으고, 하단 Asset Tray 썸네일 클릭 시 메인 이미지 레이어를 즉시 교체하는 UI 구현
- [x] **Vite 실행 뼈대 복구**: `package.json`, `index.html`, `src/main.jsx`를 추가해 현재 단일 파일 구조를 유지한 채 로컬 실행과 배포 연결이 가능하도록 정리
- [x] **에이전트 기반 자동 텍스트 요약 (Agentic Feature)**: 텍스트 레이어의 내용이 Safe Area를 벗어날 경우, LLM 하위 에이전트를 호출해 짧은 카피로 자동 요약/대체하는 기능 (API 연동 뼈대 및 UI 완료)
- [x] **[Hotfix]** 스페이스바 클릭 시 레이어 선택 무시 및 패닝(이동) 이벤트 버블링 단일화
- [x] **커스텀 웹폰트 로더**: 사용자가 웹폰트명을 직접 입력하여 `document.fonts.ready`에 동적으로 추가하는 기능
- [x] **Context Menu (우클릭)**: 캔버스 레이어 및 아트보드 우클릭 시 위치 복원, 레이어 순서(z-index) 변경, 복제, 삭제 등의 유틸리티 메뉴 추가
- [x] **브랜드 테마 프리셋**: 버튼 클릭 한 번으로 `bgColor`, 기본 `fontFamily`, `CTA 배경색` 등을 특정 브랜드 가이드라인에 맞게 일괄 변경하는 기능
- [x] 휠 드래그(마우스 휠) 패닝 시 레이어 및 아트보드 중복 선택 방지 (`e.button !== 0` 필터링)
- [x] 이미지 마스킹/크롭 컨트롤러
- [x] 비율 감지 스마트 레이아웃 자동화 (computeLayout v2.0)
- [x] Safe Area 경계 기준 다중 정렬 (DOM ClientRect 기반 역산)
- [x] 1:1 정방형 이미지 비율 고정 스케일링
- [x] 아트보드별 요소 개별 숨김 및 템플릿 기본값 저장(Save as Default)
- [x] 50단계 Undo/Redo (History 엔진)
- [x] 3px 이하 마이크로 텍스트 렌더링 누락 문제(보드 스케일링 이슈) 해결
- [x] **다크 모드 복귀 + Transform 핸들 보정**: 전체 편집기 표면을 다시 다크 톤으로 정리하고, 선택 박스는 0.5px 보더 기반으로 밀착시킴. 상하좌우/우하단 핸들을 3px dot과 0.5px 흰색 스트로크로 통일하고, 우하단 스케일이 실제 요소 크기와 정비례하도록 비율식으로 수정
- [x] **줌/패닝/리사이즈 감도 상향**: 휠 줌을 지수 기반 포인터 중심 스텝으로 교체하고, 패닝과 레이어 이동/리사이즈 드래그 배수를 올려 더 적은 입력으로도 충분한 변화가 일어나도록 조정
- [x] **아트보드 직접 텍스트 편집**: 선택된 텍스트 레이어가 캔버스 위에서 즉시 textarea로 바뀌어 우측 패널을 거치지 않고 바로 카피 수정이 가능하도록 개선
- [x] **인플레이스 텍스트 편집 안정화**: textarea 치환 대신 contentEditable 기반으로 바꿔 현재 레이어 표시 상태를 유지한 채 아트보드 위에서 직접 타이핑 가능하게 정리. Enter는 줄바꿈, Escape는 수정값을 유지한 채 편집 모드만 종료
- [x] **선택 외곽 드래그 링 + 사각형 핸들**: 선택 스트로크 바깥 1px 투명 드래그 링을 추가해 이동 잡기가 쉬워지도록 하고, 핸들은 4px 스퀘어 형태로 변경
- [x] **핸들 더블탭 자동 맞춤 + CTA 정렬 보정**: 핸들을 더블탭하면 텍스트/CTA 콘텐츠 실측 크기에 맞춰 레이어 박스를 잉여공간 없이 맞추고, CTA 버튼 텍스트는 수직 중앙 정렬과 기본 화살표 제거를 함께 적용
- [x] **텍스트 편집 이벤트 충돌 수정**: 편집 중에는 이동용 투명 오버레이를 비활성화하고 contentEditable에 자동 포커스를 넣어 선택 직후 바로 타이핑되도록 보정
- [x] **캔버스 이미지 체크보드 제거**: PNG 업로드 후에는 이미지 레이어 기본 배경을 투명 처리해 편집 캔버스에서 격자무늬가 보이지 않도록 정리
- [x] **한글 IME 입력 안정화**: contentEditable 입력 중에는 DOM/ref에만 내용을 유지하고 blur/Escape 시점에만 상태를 반영하도록 바꿔 조합형 한글 타이핑 깨짐 문제를 수정
- [x] **텍스트 기본 박스 실측화**: 오버라이드 없는 텍스트/CTA 레이어는 콘텐츠 실측 폭과 높이를 기본 박스로 사용해 선택 UI가 실제 요소 크기에 더 가깝게 맞도록 조정
- [x] **바깥 클릭/ESC 편집 종료 통일**: 레이어 밖 클릭 또는 탭, Escape 모두 현재 수정값을 유지한 채 편집 종료와 선택 해제가 함께 일어나도록 정리
- [x] **CTA 패딩 20%/30% 적용**: `자세히 보기` 버튼 여백을 폰트 크기 기준 상하 20%, 좌우 30%로 맞춰 캔버스와 export 결과를 통일
- [x] **이미지/CTA 선택 규칙 통일**: 바깥 클릭과 Escape 선택 해제 흐름을 이미지 레이어와 CTA 버튼까지 확장해 레이어 타입별 동작 차이를 줄임
- [x] **선택 시 레이어명 라벨 표시**: 선택된 레이어 좌상단에 작은 이름 라벨을 띄우고, 이미지 영역 중앙 플레이스홀더 텍스트는 제거해 캔버스를 덜 어지럽게 정리
- [x] **멀티라인 높이 실시간 반영**: 편집 중인 임시 문자열 기준으로 텍스트 실측 높이를 다시 계산해 두 줄 이상 입력 시 선택 박스 높이가 바로 따라오도록 수정
- [x] **중간정리/자동루프/완성체크리스트 문서화**: 현재 상태, 배운 점, 남은 과제, 반복 작업 루프를 별도 MD로 정리해 다른 기기와 다음 세션에서도 바로 이어갈 수 있게 구조화
- [x] **텍스트 실측/레이어명 편집 보정**: `line-height` 기반 선택 박스 계산을 DOM 실측으로 교체하고, 선택 드래그 인식 영역 확대, 선택 라벨 축소/파란색 통일, `(변형됨)`/`초기화` 칩 리디자인, 좌측 패널 더블클릭 레이어명 편집 추가
- [x] **인플레이스 입력/드래그 충돌 보정**: `contentEditable`에서 스페이스 입력이 패닝으로 탈취되지 않게 하고, 선택선과 레이어명 라벨 자체를 드래그 가능한 이동 영역으로 확장, 줄바꿈 추가/삭제 시 선택 박스가 즉시 늘고 줄어들도록 리렌더 타이밍 보정
- [x] **contentEditable 줄 구조 파싱 보강**: `div/p/br` 기반 줄바꿈 DOM을 직접 파싱하고 `requestAnimationFrame` 타이밍으로 재측정해 엔터 직후에도 선택 박스 높이가 더 빠르게 따라오도록 조정
- [x] **자동 실측 vs 수동 박스 분리**: 텍스트 레이어는 기본적으로 내용 실측 크기를 따르되, 사용자가 transform 리사이즈를 한 경우에만 `manualBox`로 고정 폭/높이를 유지하도록 분리해 줄바꿈 시 세로 붕괴를 방지
- [x] **텍스트 편집 엔진 안정화 전환**: `contentEditable` 대신 투명 `textarea` 오버레이를 사용해 스페이스/엔터/줄바꿈 입력을 안정화하고, 기존 레이어 위치와 스타일을 유지한 채 직접 편집 흐름을 보존
- [x] **선택 진입 즉시 draft 동기화**: 텍스트 레이어를 클릭하는 순간 현재 보드의 content를 draft 상태에 즉시 복사해 첫 렌더부터 줄 수 기준 선택 박스가 잡히게 하고, 편집 오버레이 스크롤바는 강제로 숨김

## 2026-04-06 Modularization Loop
- [x] First-pass modularization started: reusable `NumberInput`, `ResizeHandles`, and `LayerListPanel` moved under `src/adcanvas/components/`.
- [ ] Next modularization pass: move bottom `Asset Tray`, board inspector blocks, and board rendering helpers out of `App.jsx`.
- [ ] Consolidate remaining duplicated measurement and selection logic behind shared helpers so text/edit bugs are fixed in one place.
- [x] 텍스트 편집 상태와 표시 상태 줄바꿈 규칙 통일: 기본 텍스트는 명시적 줄바꿈만 유지하고, `manualBox` 상태에서만 폭 기준 자동 줄바꿈을 허용하도록 조정.
- [x] 수동 리사이즈 텍스트는 폭만 유지하고 높이는 내용만큼 자동 확장: Enter로 줄이 늘어나면 상단 기준을 유지한 채 아래로 레이어가 같이 커지도록 조정.
- [x] 텍스트/CTA는 높이 리사이즈 제거: 좌우 폭 조절과 우하단 비율 스케일만 허용하고, 높이는 항상 내용 기준 자동 확장으로 통일.

## 2026-04-06 Artboard Size Input
- [x] Added direct numeric width / height inputs for the currently selected artboard in the right inspector.
- [x] Introduced board-scoped `boardSizeOverrides` so board rendering, measurement, image ratio math, and labels all resolve through one size source.
- [x] Board reset now removes size overrides together with layer overrides and board defaults.
- [x] Tightened the artboard size inspector inputs so values do not look clipped in the narrow right panel, and added lightweight pressed-state feedback to buttons.
- [x] Added cross-platform compatibility guidance for Chrome / Safari-WebKit and hardened number inputs, textarea scrollbar hiding, header blur, and resize handles with WebKit-safe fallbacks.
- [x] Disabled state pass: the layer panel now deactivates visually and functionally until an artboard is selected, and the properties panel shows a disabled placeholder when no artboard context exists.
- [x] Added active artboard ratio input, fixed multi-digit NumberInput drafting, cleared artboard selection on empty canvas clicks, and stabilized the first inline-text Enter expansion via textarea scrollHeight syncing.
- [x] Replaced decimal ratio input with width:height ratio controls so artboard ratio can be edited as values like 9:16 instead of 0.56 or 0.1.
- [x] Renamed the editor header to `Banner Variation Editor` and removed the legacy subtitle.
- [x] Let CTA layers enter direct inline editing on the artboard and aligned CTA selector bounds to the visible pill button.
- [x] Restored vertical resize handles for text-like layers and fixed multiline selector measurement so trailing blank lines are reflected while editing.
- [x] Added transform badges before layer names in the canvas labels and the layer panel.
- [x] Replaced the bottom URL asset import tray with a saved-history panel for full workspace save and load.
- [x] Save entries now default to the current date and time, support custom names, and store a square-banner thumbnail when available.
- [x] Load actions now show a confirmation warning that unsaved current-canvas changes may be lost before the snapshot is applied.
- [x] Removed unused legacy helpers and stale refs left behind by older edit and asset flows.
- [x] Optimized undo/save history serialization so repeated state snapshots do less redundant JSON work.
- [x] Unified the typography `Size` input with the rendered `fs` value so numeric font edits and transform scaling stay aligned.
- [x] Removed the extra transform badge decoration beside layer names in both the canvas and layer panel.
- [x] Moved active artboard emphasis to an outside selection stroke that does not consume inner board area.
- [x] Added default `780x780 / LGE_heroBanner` and changed the initial open state so only that artboard is selected on first load.
- [x] Changed safe-area guides to default `off` and simplified the canvas grid to the center crosshair only.
- [x] Added export destination picking through the browser file-system API with download fallback for unsupported browsers.
- [x] Added eyedropper shortcut buttons next to the text-color and CTA background color inputs.
- [x] Expanded selected-layer stroke dragging so the visible blue edge has a larger reliable move hit area.
- [x] Added `Pretendard` as a selectable font option and load path for Korean typography work.
- [x] Converted tracking to `-30%` to `+30%` control around `0%`, and converted line-height controls to percent-based input with `140%` as the default baseline.
- [x] Clarified typography inspector units with `Size (px)` and `Weight (100-900)` labels.
- [x] Moved board-scoped layer deletion into the left layer panel as a per-layer trash icon and removed the duplicate destructive button from the right inspector.
- [x] Made the selected-layer outer companion stroke transparent while keeping the widened draggable selector band.
- [x] Removed the redundant text-role header and dropdown from the right inspector, and renamed the section to `텍스트 편집창`.
- [x] Localized typography labels in the right inspector to `폰트사이즈 (px)` and `폰트웨이트 (100-900)`.
